<%= form_with(model: lion, local: true) do |form| %>
  <% if lion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lion.errors.count, "error") %> prohibited this lion from being saved:</h2>

      <ul>
      <% lion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :lion_name %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :lion_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
