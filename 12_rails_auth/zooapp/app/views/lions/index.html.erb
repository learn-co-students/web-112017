<p id="notice"><%= notice %></p>

<h1>Lions</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lions.each do |lion| %>
      <tr>
        <td><%= lion.name %></td>
        <td><%= lion.user.username %></td>
        <td><%= link_to 'Show', lion %></td>
        <td><%= link_to 'Edit', edit_lion_path(lion) %></td>
        <td><%= link_to 'Destroy', lion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lion', new_lion_path %>
